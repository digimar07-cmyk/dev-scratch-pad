"""\nui/controllers/project_management_controller.py — Gerencia operações de projetos.\n\nFASE 7C.3: Extrai lógica de gerenciamento de projetos do main_window.py\nRedução estimada: -64 linhas no main_window.py\n"""\nimport os\nfrom tkinter import messagebox\n\n\nclass ProjectManagementController:\n    \"\"\"\n    Controller para gerenciar operações em projetos individuais.\n    \n    Responsabilidades:\n    - Remover projetos do banco\n    - Limpar projetos órfãos\n    - Toggle de flags (favorite, done, good, bad)\n    \"\"\"\n    \n    def __init__(self, database, db_manager, collections_manager):\n        self.database = database\n        self.db_manager = db_manager\n        self.collections_manager = collections_manager\n        \n        # Callbacks para UI\n        self.on_project_removed = None     # callback(str: project_name)\n        self.on_orphans_cleaned = None     # callback(int: count)\n        self.on_status_message = None      # callback(str: message)\n        self.on_refresh_needed = None      # callback()\n    \n    def remove_project(self, path):\n        \"\"\"Remove um projeto do banco.\"\"\"\n        if path not in self.database:\n            return False\n        \n        name = self.database[path].get(\"name\", os.path.basename(path))\n        \n        # Remover do banco\n        self.database.pop(path)\n        self.db_manager.save_database()\n        \n        # Limpar de coleções\n        self.collections_manager.clean_orphan_projects(set(self.database.keys()))\n        \n        if self.on_status_message:\n            self.on_status_message(f\"🗑️ '{name}' removido do banco.\")\n        \n        if self.on_project_removed:\n            self.on_project_removed(name)\n        \n        if self.on_refresh_needed:\n            self.on_refresh_needed()\n        \n        return True\n    \n    def clean_orphans(self, parent_window):\n        \"\"\"Remove projetos órfãos (pastas que não existem mais).\"\"\"\n        # Encontrar órfãos\n        orphans = [p for p in self.database.keys() if not os.path.isdir(p)]\n        \n        if not orphans:\n            messagebox.showinfo(\n                \"✅ Banco limpo\",\n                \"Nenhum órfão encontrado!\\n\\nTodos os projetos têm pastas válidas.\",\n                parent=parent_window\n            )\n            return 0\n        \n        # Primeira confirmação\n        msg = f\"Encontrei {len(orphans)} projeto(s) órfão(s):\\n\\n\"\n        msg += \"\\n\".join(f\"- {os.path.basename(p)}\" for p in orphans[:10])\n        if len(orphans) > 10:\n            msg += f\"\\n... e mais {len(orphans) - 10}\"\n        msg += \"\\n\\nEsses projetos não existem mais no disco.\\nRemover do banco?\"\n        \n        if not messagebox.askyesno(\"🧹 Limpar órfãos\", msg, icon=\"warning\", parent=parent_window):\n            return 0\n        \n        # Segunda confirmação\n        if not messagebox.askyesno(\n            \"⚠️ Confirmar remoção\",\n            f\"Segunda confirmação.\\n\\n{len(orphans)} projeto(s) serão removidos PERMANENTEMENTE do banco.\\n\\nTem certeza?\",\n            icon=\"warning\",\n            parent=parent_window\n        ):\n            return 0\n        \n        # Remover órfãos\n        for path in orphans:\n            self.database.pop(path, None)\n        \n        self.db_manager.save_database()\n        self.collections_manager.clean_orphan_projects(set(self.database.keys()))\n        \n        if self.on_status_message:\n            self.on_status_message(f\"🧹 {len(orphans)} órfão(s) removido(s) do banco.\")\n        \n        if self.on_orphans_cleaned:\n            self.on_orphans_cleaned(len(orphans))\n        \n        if self.on_refresh_needed:\n            self.on_refresh_needed()\n        \n        # Mensagem final\n        messagebox.showinfo(\n            \"✅ Limpeza concluída\",\n            f\"{len(orphans)} projeto(s) órfão(s) removido(s) do banco.\\n\\nBanco agora está sincronizado com o disco.\",\n            parent=parent_window\n        )\n        \n        return len(orphans)\n    \n    def toggle_flag(self, path, flag_name):\n        \"\"\"\n        Alterna flag de um projeto (favorite, done, good, bad).\n        \n        Args:\n            path: Caminho do projeto\n            flag_name: Nome da flag ('favorite', 'done', 'good', 'bad')\n        \n        Returns:\n            bool: Novo valor da flag (ou None se falhou)\n        \"\"\"\n        if path not in self.database:\n            return None\n        \n        # Obter valor atual\n        current_value = self.database[path].get(flag_name, False)\n        new_value = not current_value\n        \n        # Atualizar flag\n        self.database[path][flag_name] = new_value\n        \n        # Regras especiais: good/bad são mutuamente exclusivos\n        if flag_name == \"good\" and new_value:\n            self.database[path][\"bad\"] = False\n        elif flag_name == \"bad\" and new_value:\n            self.database[path][\"good\"] = False\n        \n        self.db_manager.save_database()\n        \n        return new_value\n    \n    def get_flag_value(self, path, flag_name):\n        \"\"\"Retorna valor de uma flag de um projeto.\"\"\"\n        if path not in self.database:\n            return False\n        return self.database[path].get(flag_name, False)\n