"""\nUtils — Image Loader\nCarregamento de imagens (capa, hero, galeria)\n"""\n\nimport os\nfrom PIL import Image, ImageTk\nfrom config import LOGGER\n\n\nclass ImageLoader:\n    @staticmethod\n    def find_first_image_path(project_path):\n        """Retorna caminho da primeira imagem válida encontrada."""\n        valid_extensions = (".png", ".jpg", ".jpeg", ".gif", ".bmp", ".webp")\n        try:\n            for item in os.listdir(project_path):\n                if item.lower().endswith(valid_extensions):\n                    return os.path.join(project_path, item)\n        except Exception:\n            LOGGER.exception(f"Falha ao listar {project_path}")\n        return None\n\n    @staticmethod\n    def load_thumbnail_photo(img_path, size=(220, 200)):\n        """Carrega imagem como thumbnail."""\n        try:\n            img = Image.open(img_path)\n            img.thumbnail(size, Image.Resampling.LANCZOS)\n            return ImageTk.PhotoImage(img)\n        except Exception:\n            LOGGER.exception(f"Erro ao gerar thumbnail de {img_path}")\n            return None\n\n    @staticmethod\n    def get_hero_image(project_path, max_width=800):\n        """Carrega imagem hero (grande) para modal."""\n        try:\n            valid_extensions = (".png", ".jpg", ".jpeg", ".gif", ".bmp")\n            for item in os.listdir(project_path):\n                if item.lower().endswith(valid_extensions):\n                    img_path = os.path.join(project_path, item)\n                    img = Image.open(img_path)\n                    if img.width > max_width:\n                        ratio = max_width / img.width\n                        img = img.resize((max_width, int(img.height * ratio)), Image.Resampling.LANCZOS)\n                    return ImageTk.PhotoImage(img)\n        except Exception:\n            LOGGER.exception(f"Falha ao carregar hero image de {project_path}")\n        return None\n\n    @staticmethod\n    def get_all_project_images(project_path):\n        """Retorna lista de todas as imagens do projeto."""\n        valid_extensions = (".png", ".jpg", ".jpeg", ".gif", ".bmp")\n        images = []\n        try:\n            for item in os.listdir(project_path):\n                if item.lower().endswith(valid_extensions):\n                    images.append(os.path.join(project_path, item))\n        except Exception:\n            LOGGER.exception(f"Falha ao listar imagens de {project_path}")\n        return sorted(images)\n