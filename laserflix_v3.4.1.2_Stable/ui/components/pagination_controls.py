"""\nui/components/pagination_controls.py — Controles de paginação e ordenação.\n\nFASE 7D.3: Extrai controles de paginação do main_window.py\nRedução estimada: -57 linhas no main_window.py\n"""\nimport tkinter as tk\nfrom tkinter import ttk\nfrom ui.theme import BG_PRIMARY, FG_PRIMARY, FG_SECONDARY, ACCENT_BLUE\n\n\nclass PaginationControls:\n    \"\"\"\n    Component para controles de paginação e ordenação.\n    \n    Responsabilidades:\n    - Dropdown de ordenação\n    - Botões de navegação (primeira, anterior, próxima, última)\n    - Label de página atual\n    \"\"\"\n    \n    def __init__(self, parent):\n        self.parent = parent\n        \n        # Frame principal\n        self.frame = tk.Frame(parent, bg=BG_PRIMARY)\n        \n        # Container esquerdo (ordenação)\n        left_container = tk.Frame(self.frame, bg=BG_PRIMARY)\n        left_container.pack(side=\"left\", padx=10)\n        \n        # Label \"Ordenar por:\"\n        sort_label = tk.Label(\n            left_container,\n            text=\"Ordenar por:\",\n            bg=BG_PRIMARY,\n            fg=FG_SECONDARY,\n            font=(\"Segoe UI\", 9)\n        )\n        sort_label.pack(side=\"left\", padx=(0, 5))\n        \n        # Combobox de ordenação\n        self.sort_combo = ttk.Combobox(\n            left_container,\n            state=\"readonly\",\n            width=18,\n            font=(\"Segoe UI\", 9)\n        )\n        self.sort_combo.pack(side=\"left\")\n        \n        # Container direito (navegação)\n        right_container = tk.Frame(self.frame, bg=BG_PRIMARY)\n        right_container.pack(side=\"right\", padx=10)\n        \n        # Botão primeira página\n        self.first_btn = tk.Button(\n            right_container,\n            text=\"⏮\",\n            bg=\"#2A2A3E\",\n            fg=FG_PRIMARY,\n            font=(\"Segoe UI\", 10),\n            width=3,\n            relief=\"flat\",\n            cursor=\"hand2\"\n        )\n        self.first_btn.pack(side=\"left\", padx=2)\n        \n        # Botão página anterior\n        self.prev_btn = tk.Button(\n            right_container,\n            text=\"◀\",\n            bg=\"#2A2A3E\",\n            fg=FG_PRIMARY,\n            font=(\"Segoe UI\", 10),\n            width=3,\n            relief=\"flat\",\n            cursor=\"hand2\"\n        )\n        self.prev_btn.pack(side=\"left\", padx=2)\n        \n        # Label de página atual\n        self.page_label = tk.Label(\n            right_container,\n            text=\"Página 1 de 1\",\n            bg=BG_PRIMARY,\n            fg=FG_PRIMARY,\n            font=(\"Segoe UI\", 9, \"bold\"),\n            width=15\n        )\n        self.page_label.pack(side=\"left\", padx=8)\n        \n        # Botão próxima página\n        self.next_btn = tk.Button(\n            right_container,\n            text=\"▶\",\n            bg=\"#2A2A3E\",\n            fg=FG_PRIMARY,\n            font=(\"Segoe UI\", 10),\n            width=3,\n            relief=\"flat\",\n            cursor=\"hand2\"\n        )\n        self.next_btn.pack(side=\"left\", padx=2)\n        \n        # Botão última página\n        self.last_btn = tk.Button(\n            right_container,\n            text=\"⏭\",\n            bg=\"#2A2A3E\",\n            fg=FG_PRIMARY,\n            font=(\"Segoe UI\", 10),\n            width=3,\n            relief=\"flat\",\n            cursor=\"hand2\"\n        )\n        self.last_btn.pack(side=\"left\", padx=2)\n        \n        # Callbacks\n        self.on_sort_changed = None\n        self.on_first = None\n        self.on_prev = None\n        self.on_next = None\n        self.on_last = None\n        \n        # Conectar eventos\n        self.sort_combo.bind(\"<<ComboboxSelected>>\", lambda e: self._on_sort_changed())\n        self.first_btn.config(command=self._on_first_clicked)\n        self.prev_btn.config(command=self._on_prev_clicked)\n        self.next_btn.config(command=self._on_next_clicked)\n        self.last_btn.config(command=self._on_last_clicked)\n        \n        # Hover effects\n        self._setup_hover_effects()\n    \n    def _setup_hover_effects(self):\n        \"\"\"Configura efeitos de hover nos botões.\"\"\"\n        for btn in [self.first_btn, self.prev_btn, self.next_btn, self.last_btn]:\n            btn.bind(\"<Enter>\", lambda e, b=btn: b.config(bg=\"#3A3A4E\"))\n            btn.bind(\"<Leave>\", lambda e, b=btn: b.config(bg=\"#2A2A3E\"))\n    \n    def render(self, page_info, sort_options):\n        \"\"\"\n        Renderiza controles com estado atual.\n        \n        Args:\n            page_info: Dict com informações de paginação\n                {\n                    'current': int,\n                    'total': int,\n                    'has_prev': bool,\n                    'has_next': bool\n                }\n            sort_options: List de strings com opções de ordenação\n        \"\"\"\n        # Configurar opções de ordenação\n        self.sort_combo['values'] = sort_options\n        if sort_options:\n            self.sort_combo.current(0)\n        \n        # Atualizar estado\n        self.update_state(page_info)\n    \n    def update_state(self, page_info):\n        \"\"\"\n        Atualiza estado dos controles de navegação.\n        \n        Args:\n            page_info: Dict com informações de paginação\n        \"\"\"\n        current = page_info.get('current', 1)\n        total = page_info.get('total', 1)\n        has_prev = page_info.get('has_prev', False)\n        has_next = page_info.get('has_next', False)\n        \n        # Atualizar label\n        self.page_label.config(text=f\"Página {current} de {total}\")\n        \n        # Habilitar/desabilitar botões\n        first_state = \"normal\" if has_prev else \"disabled\"\n        prev_state = \"normal\" if has_prev else \"disabled\"\n        next_state = \"normal\" if has_next else \"disabled\"\n        last_state = \"normal\" if has_next else \"disabled\"\n        \n        self.first_btn.config(state=first_state)\n        self.prev_btn.config(state=prev_state)\n        self.next_btn.config(state=next_state)\n        self.last_btn.config(state=last_state)\n        \n        # Ajustar cor de botões desabilitados\n        disabled_color = \"#1A1A2E\"\n        enabled_color = \"#2A2A3E\"\n        \n        self.first_btn.config(bg=disabled_color if not has_prev else enabled_color)\n        self.prev_btn.config(bg=disabled_color if not has_prev else enabled_color)\n        self.next_btn.config(bg=disabled_color if not has_next else enabled_color)\n        self.last_btn.config(bg=disabled_color if not has_next else enabled_color)\n    \n    def get_selected_sort(self):\n        \"\"\"Retorna opção de ordenação selecionada.\"\"\"\n        return self.sort_combo.get()\n    \n    def set_selected_sort(self, value):\n        \"\"\"Define opção de ordenação.\"\"\"\n        try:\n            idx = self.sort_combo['values'].index(value)\n            self.sort_combo.current(idx)\n        except (ValueError, tk.TclError):\n            pass\n    \n    def pack(self, **kwargs):\n        \"\"\"Pack do frame principal.\"\"\"\n        self.frame.pack(**kwargs)\n    \n    def grid(self, **kwargs):\n        \"\"\"Grid do frame principal.\"\"\"\n        self.frame.grid(**kwargs)\n    \n    # Callbacks internos\n    def _on_sort_changed(self):\n        if self.on_sort_changed:\n            self.on_sort_changed(self.get_selected_sort())\n    \n    def _on_first_clicked(self):\n        if self.on_first:\n            self.on_first()\n    \n    def _on_prev_clicked(self):\n        if self.on_prev:\n            self.on_prev()\n    \n    def _on_next_clicked(self):\n        if self.on_next:\n            self.on_next()\n    \n    def _on_last_clicked(self):\n        if self.on_last:\n            self.on_last()\n