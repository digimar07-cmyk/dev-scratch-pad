"""\nui/controllers/collection_controller.py — Gerencia coleções de projetos.\n\nFASE 7C.2: Extrai toda lógica de coleções do main_window.py\nRedução estimada: -102 linhas no main_window.py\n"""\nimport os\nfrom tkinter import simpledialog, messagebox\n\n\nclass CollectionController:\n    \"\"\"\n    Controller para gerenciar coleções/playlists de projetos.\n    \n    Responsabilidades:\n    - Adicionar/remover projetos de coleções\n    - Criar novas coleções\n    - Abrir dialog de gerenciamento\n    - Aplicar filtros de coleção\n    \"\"\"\n    \n    def __init__(self, collections_manager, database, db_manager):\n        self.collections_manager = collections_manager\n        self.database = database\n        self.db_manager = db_manager\n        \n        # Callbacks para UI\n        self.on_collection_changed = None  # callback()\n        self.on_status_message = None      # callback(str: message)\n    \n    def open_dialog(self, parent_window):\n        \"\"\"Abre dialog de gerenciamento de coleções.\"\"\"\n        from ui.collections_dialog import CollectionsDialog\n        \n        parent_window.wait_window(\n            CollectionsDialog(\n                parent=parent_window,\n                collections_manager=self.collections_manager,\n                database=self.database\n            )\n        )\n        \n        if self.on_collection_changed:\n            self.on_collection_changed()\n    \n    def add_project(self, collection_name, project_path):\n        \"\"\"Adiciona projeto a uma coleção.\"\"\"\n        self.collections_manager.add_project(collection_name, project_path)\n        \n        project_name = self.database.get(project_path, {}).get(\n            \"name\", \n            os.path.basename(project_path)\n        )\n        \n        if self.on_status_message:\n            self.on_status_message(\n                f\"✅ '{project_name}' adicionado à coleção '{collection_name}'\"\n            )\n        \n        if self.on_collection_changed:\n            self.on_collection_changed()\n    \n    def remove_project(self, collection_name, project_path):\n        \"\"\"Remove projeto de uma coleção.\"\"\"\n        self.collections_manager.remove_project(collection_name, project_path)\n        \n        project_name = self.database.get(project_path, {}).get(\n            \"name\",\n            os.path.basename(project_path)\n        )\n        \n        if self.on_status_message:\n            self.on_status_message(\n                f\"🗑️ '{project_name}' removido da coleção '{collection_name}'\"\n            )\n        \n        if self.on_collection_changed:\n            self.on_collection_changed()\n    \n    def create_with_project(self, project_path, parent_window):\n        \"\"\"Cria nova coleção com um projeto inicial.\"\"\"\n        name = simpledialog.askstring(\n            \"📁 Nova Coleção\",\n            \"Nome da nova coleção:\",\n            parent=parent_window\n        )\n        \n        if not name or not name.strip():\n            return None\n        \n        name = name.strip()\n        \n        # Validar se já existe\n        if name in self.collections_manager.collections:\n            messagebox.showerror(\n                \"Erro\",\n                f\"Coleção '{name}' já existe!\\n\\nEscolha outro nome.\",\n                parent=parent_window\n            )\n            return None\n        \n        # Criar coleção\n        self.collections_manager.add_collection(name)\n        self.collections_manager.add_project(name, project_path)\n        \n        project_name = self.database.get(project_path, {}).get(\n            \"name\",\n            os.path.basename(project_path)\n        )\n        \n        if self.on_status_message:\n            self.on_status_message(\n                f\"📁 Coleção '{name}' criada com '{project_name}'\"\n            )\n        \n        if self.on_collection_changed:\n            self.on_collection_changed()\n        \n        return name\n    \n    def get_all_collections(self):\n        \"\"\"Retorna lista de todas as coleções.\"\"\"\n        return list(self.collections_manager.collections.keys())\n    \n    def get_project_collections(self, project_path):\n        \"\"\"Retorna lista de coleções que contêm o projeto.\"\"\"\n        return self.collections_manager.get_project_collections(project_path)\n    \n    def get_collection_size(self, collection_name):\n        \"\"\"Retorna número de projetos na coleção.\"\"\"\n        return self.collections_manager.get_collection_size(collection_name)\n