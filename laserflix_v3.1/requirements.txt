# LASERFLIX v3.0 - Dependências Python

# Processamento de imagens
Pillow>=10.0.0

# Requisições HTTP (Ollama API)
requests>=2.31.0

# Tkinter já vem incluso no Python padrão
# Se usar Python via pyenv/conda e tiver problemas, instale:
# Ubuntu/Debian: sudo apt-get install python3-tk
# macOS: brew install python-tk
# Windows: Tkinter já incluso
